apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitlab-runner-maven-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  volumeName: gitlab-runner-maven-pv
